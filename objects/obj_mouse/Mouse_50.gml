/// @description drag and drop option (WILL NOT USE, I FOUND A BETTER METHOD)

/*
if(firstDown)
{
	x1 = mouse_x;
	y1 = mouse_y;
	firstDown = false;
}
else
{
	x2 = mouse_x;
	y2 = mouse_y;
	show_debug_message("x1 = " + string(x1) + " - " + "x2 = " + string(x2) + " is change x:" + string(x1 -x2));
	if(item == noone)
	{
		x += (dragSpeed * (x1 - x2));
		y += (dragSpeed * (y1 - y2));
	} 
    firstDown = true;
}
*/